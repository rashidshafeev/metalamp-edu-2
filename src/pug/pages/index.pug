extends ../layout/main

block variables
  - var title = 'Webpack + Pug';
  - var description = 'Description for Webpack + Pug';
  - var pageClass = 'index-page';

block content
  section
    .container
      +item('item title ololo', 'item descr')
      +item('item title', 'item descr')
      +item('item title', 'item descr')
      .test_block
        img(src='../../img/logo.svg')
  example-component


block script
  script(type='text/javascript').
    const foo = true;
    let bar = function() {};
    if (foo) {
    bar(1 + 5)
    }